version: '3.8'

services:
  wallpaper-app:
    build: .
    ports:
      - "3660:5000"
    volumes:
          - /vol2/1000/Wallpapers:/app/backend/wallpapers
    environment:
          - PYTHONPATH=/app
          - FLASK_ENV=production
          - FLASK_APP=backend/app.py
          - FLASK_RUN_HOST=0.0.0.0
          - FLASK_RUN_PORT=5000
    restart: unless-stopped
    container_name: wallpaper-app